# @bke/privacy

Reusable, host-independent privacy-request policy for BKE applications.

The package owns canonical request types and statuses, request creation normalization, transition rules, response requirements, and event/audit mutation intent. Consumers own HTTP/network extraction, timestamps, persistence, transactions, and delivery/operations.
